# Personal Website

Next.js personal homepage configured for static export and GitHub Pages.

## Development

```bash
npm install
npm run dev
```

## Deploy

Push to the `main` branch on GitHub. The GitHub Actions workflow builds the site into `out/` and publishes it with GitHub Pages.
